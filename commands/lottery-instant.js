// Superseded by commands/lottery.js ("/복권 긁기" subcommand), which merges
// this together with the draw-style lottery under one /복권 parent command.
// Kept as a deprecated stub (rather than deleted) since this environment has
// no file-delete capability - the loader in index.js and deploy-commands.js
// both skip any command with `deprecated: true`.
module.exports = { deprecated: true };
