// Deprecated: this command referenced an undefined `client` variable (see the
// original file's own trailing comment "// Client is not defined.") and never
// worked. Removed rather than converted since there's no working behavior to carry
// forward.
//
// Safe to delete this file.
module.exports = { deprecated: true };
